 Animate a set of bars that shrink as you apply Huffman or LZW compression to sample text, showing the entropy limit approaching Shannon’s First Theorem.

  Use SVG constellations that morph between PSK, QAM, and APSK arrangements, with tooltips explaining why symbol placement changes.

  Build an interactive Shannon limit explorer where a slider adjusts SNR and the channel capacity curve updates in real time beside simulated BER plots for different
  codes.

  Create a Nyquist pulse visualizer: toggle SRRC roll-off factors and watch overlapping pulses either interfere or line up perfectly at symbol decision points.

  Render an OFDM spectrum in WebGL with sliders for subcarrier spacing and cyclic prefix length, showing spectral leakage and the guard interval effect.

  Design an interactive LTE frame grid where each resource element lights up as PBCH, PDCCH, PDSCH, etc., while a timeline illustrates FDD vs. TDD slot usage.
